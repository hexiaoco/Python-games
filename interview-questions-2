
3-22 Wednesday 10:52

1. 使用列表推导式和 filter/lambda 函数式2中方式输出10以内的偶数。

2. 写出下面代码的输出。

	a = [1,2,3]
	li = [a] * 3
	print(a,li)
	li[0].append(4)
	print(a,li)

3. 写出下面代码的输出。

	def func():
		return [lambda x: x * i for i in range(0,4)]
	print(m(2) for m in func()])

4. 写一个输出3的倍数的生成器函数。

5. 写一个验证邮箱格式是否正确的函数。

6. 简述AOP概念，举例说明AOP在python中的实现。

7. SQL中內连和外连的区别，并分别写出一条伪sql或语法格式。

8. 写出一个项目中使用的数据库类型及ORM框架，框架链接数据库时是否使用了连接池。

9. 根据一个熟悉的Web框架，简要描述一次完整的HTTP请求处理过程。

10. 如何提高Web服务响应速度？


